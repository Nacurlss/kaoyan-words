# 题型筛选功能实现计划

> **Goal:** 增加按题型（全部/完形填空/阅读理解A+B/翻译题）筛选词频统计的能力

**Architecture:** 设置驱动 + 索引缓存。在现有 `_session_settings` 中加 `section_filter` 字段，`_build_index()` 入口加 section 过滤逻辑，用 `_index_cache` 缓存不同过滤组合的索引。前端在主工具栏加 select 下拉切换题型。

**Tech Stack:** Python FastAPI + React TypeScript（无新依赖）

**涉及文件:**
- Modify: `src/backend/main.py`
- Modify: `src/frontend/src/types.ts`
- Modify: `src/frontend/src/App.tsx`

---

### Task 1: 后端 — 添加 section_filter 及索引缓存

**Files:**
- Modify: `src/backend/main.py`

- [ ] **Step 1: 在 `_session_settings` 中加 `section_filter` 默认值**

找到 `_session_settings` 定义（约第 57-64 行），加一行：

```python
_session_settings = {
    "high_threshold": 0.3,
    "medium_threshold": 0.1,
    "exclude_levels": ["primary", "zhongkao"],
    "exclude_groups": [],
    "use_momo_examples": False,
    "personal_vocab_enabled": False,
    "section_filter": "all",
}
```

- [ ] **Step 2: 在 `_session_settings` 下方添加 `SECTION_FILTER_MAP` 和 `_index_cache`**

在 `_session_settings` 之后加：

```python
SECTION_FILTER_MAP = {
    "all": None,
    "cloze": ["use_of_english"],
    "reading": ["reading_a", "reading_b"],
    "translation": ["reading_c"],
}

_index_cache: dict = {}
```

- [ ] **Step 3: 添加 `_make_cache_key()` 函数**

在 `_index_cache` 之后加：

```python
def _make_cache_key() -> tuple:
    settings = _session_settings
    return (
        settings.get("section_filter", "all"),
        tuple(sorted(settings.get("exclude_levels", []))),
        tuple(sorted(settings.get("exclude_groups", []))),
        tuple(sorted(
            p["filename"] for p in _session_papers if p.get("enabled", True)
        )),
    )
```

- [ ] **Step 4: 改造 `_build_index()` — 加缓存 + section 过滤**

找到 `_build_index()` 函数（约第 180 行），替换为：

```python
def _build_index() -> dict:
    """Rebuild the full frequency index with section filter and cache."""
    global _session_papers, _session_settings, _index_cache

    cache_key = _make_cache_key()
    if cache_key in _index_cache:
        return _index_cache[cache_key]

    active_papers = [p for p in _session_papers if p.get('enabled', True)]
    if not active_papers:
        return {}

    sf = _session_settings.get("section_filter", "all")
    allowed = SECTION_FILTER_MAP.get(sf)
    if allowed is not None:
        active_papers = [p for p in active_papers if p['section'] in allowed]

    word_index = analyzer.build_frequency_index(active_papers)

    # Momo group filter
    exclude_groups = _session_settings.get('exclude_groups', [])
    if exclude_groups:
        excluded_lemmas = set()
        for g in exclude_groups:
            excluded_lemmas.update(MOMO_GROUPS.get(g, set()))
        word_index = {k: v for k, v in word_index.items() if k not in excluded_lemmas}

    # Basic word filter
    exclude = _get_exclude_words(_session_settings.get('exclude_levels', []))
    if exclude:
        word_index = apply_basic_word_filter(word_index, exclude)

    word_index = annotate_word_index(word_index)

    _index_cache[cache_key] = word_index
    return word_index
```

- [ ] **Step 5: 修改 `update_settings` — 支持 `section_filter` 并清缓存逻辑**

找到 `update_settings` 函数（约第 479-495 行），替换为：

```python
@app.put("/api/settings")
async def update_settings(settings: dict):
    """Update settings and rebuild index."""
    global _session_settings, _session_word_index

    for key in ["high_threshold", "medium_threshold", "exclude_levels",
                 "exclude_groups", "use_momo_examples", "personal_vocab_enabled",
                 "section_filter"]:
        if key in settings:
            _session_settings[key] = settings[key]

    _session_word_index = _build_index()
    return _session_settings
```

- [ ] **Step 6: 上传/删除试卷时清空 `_index_cache`**

找到 `upload_papers` 函数中的 `_session_word_index = _build_index()` 行（约第 304 行），在这行之前加：

```python
    _index_cache.clear()
```

找到 `delete_paper` 函数中的 `_session_word_index = _build_index()` 行（约第 348 行），在这行之前加：

```python
    _index_cache.clear()
```

- [ ] **Step 7: 导出文件名加题型前缀**

找到 CSV 导出 `Content-Disposition` 行（约第 766 行）：

```python
headers={"Content-Disposition": f"attachment; filename=words_{band}.csv"},
```

替换为：

```python
sf = _session_settings.get("section_filter", "all")
sf_label = {"all": "all", "cloze": "cloze", "reading": "reading",
            "translation": "translation"}.get(sf, "all")
headers={"Content-Disposition": f"attachment; filename=words_{sf_label}_{band}.csv"},
```

找到 Excel 导出的 `Content-Disposition` 行（约第 824 行附近），如上同样修改，将 `.csv` 改为 `.xlsx`。

- [ ] **Step 8: 验证后端工作正常**

```bash
# 检查当前进度确认服务健康
curl -s http://localhost:8000/api/translate/progress

# 查看 settings 是否包含 section_filter
curl -s http://localhost:8000/api/settings | python3 -m json.tool | grep section_filter

# 测试切换题型
curl -s -X PUT http://localhost:8000/api/settings \
  -H "Content-Type: application/json" \
  -d '{"section_filter": "reading"}' | python3 -m json.tool | grep section_filter

# 验证词表数据量变化
curl -s "http://localhost:8000/api/words?band=all&sort_by=frequency" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'total={d[\"total\"]}')"

# 切回全部
curl -s -X PUT http://localhost:8000/api/settings \
  -H "Content-Type: application/json" \
  -d '{"section_filter": "all"}' > /dev/null
```

---

### Task 2: 前端 — types.ts 加 SectionFilter 类型

**Files:**
- Modify: `src/frontend/src/types.ts`

- [ ] **Step 1: 在 types.ts 顶部添加类型，Settings 中加字段**

在 `export interface Settings` 上方添加：

```typescript
export type SectionFilter = "all" | "cloze" | "reading" | "translation";
```

在 `Settings` interface 中（约第 55-63 行），加一行：

```typescript
export interface Settings {
  high_threshold: number;
  medium_threshold: number;
  exclude_levels: string[];
  exclude_groups: string[];
  use_momo_examples?: boolean;
  personal_vocab_enabled?: boolean;
  available_groups?: string[];
  group_sizes?: Record<string, number>;
  section_filter?: SectionFilter;
}
```

---

### Task 3: 前端 — App.tsx 加题型切换 UI 和逻辑

**Files:**
- Modify: `src/frontend/src/App.tsx`

- [ ] **Step 1: import 中加 SectionFilter 类型**

找到 `import type { Paper, WordEntry, Settings, PersonalWord, TranslateProgress } from "./types";`（约第 3 行），改为：

```typescript
import type { Paper, WordEntry, Settings, PersonalWord, TranslateProgress, SectionFilter } from "./types";
```

- [ ] **Step 2: 添加 sectionFilter state 和切换 handler**

在 `const [preTranslating, setPreTranslating] = useState(false);` 之后（约第 44 行），加：

```typescript
  const [sectionFilter, setSectionFilter] = useState<SectionFilter>("all");

  const handleSectionFilterChange = async (value: SectionFilter) => {
    setLoading(true);
    setSelectedWord(null);
    setPage(1);
    setPersonalPage(1);

    const ns = { ...settings, section_filter: value };
    await updateSettings(ns);
    setSettings((await getSettings()));

    if (band === "personal") {
      await fetchPersonalWords();
    } else {
      await fetchWords();
    }

    setLoading(false);
  };
```

- [ ] **Step 3: 初始加载时从 settings 同步 `sectionFilter`**

找到 `fetchSettings` 函数（约第 67-70 行），在 `setSettings(s);` 之后加：

```typescript
    if (s.section_filter) {
      setSectionFilter(s.section_filter as SectionFilter);
    }
```

- [ ] **Step 4: 在 toolbar 中 band-tabs 同一行加题型 select**

找到 toolbar 中的 `<div className="band-tabs">`（约第 220 行），在 band-tabs 这个 div 之后、search-bar 之前，加题型切换选择器。也就是在 `</div>` 闭合 band-tabs 之后：

```tsx
            </div>
            <div className="section-filter">
              <span className="section-filter-label">题型：</span>
              <select
                value={sectionFilter}
                onChange={(e) => handleSectionFilterChange(e.target.value as SectionFilter)}
              >
                <option value="all">全部题型</option>
                <option value="cloze">完形填空</option>
                <option value="reading">阅读理解 A+B</option>
                <option value="translation">翻译题</option>
              </select>
            </div>
```

- [ ] **Step 5: 添加 CSS 样式**

在 `src/frontend/src/index.css` 末尾添加：

```css
.section-filter {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-left: 12px;
}

.section-filter-label {
  font-size: 13px;
  color: var(--text-secondary);
  white-space: nowrap;
}

.section-filter select {
  padding: 4px 8px;
  border: 1px solid var(--border);
  border-radius: 4px;
  font-size: 13px;
  background: var(--bg);
  color: var(--text);
  cursor: pointer;
}
```

- [ ] **Step 6: 验证前端正常工作**

```bash
# 确保前端运行
curl -s http://localhost:5173/ | head -5

# 检查 types.ts 语法
cd src/frontend && npx tsc --noEmit 2>&1 | head -20
```

---

### Task 4: 全链路验证

- [ ] **Step 1: 验证切换题型后词表变化**

后端 API：
```bash
# 全部 → 阅读理解
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"reading"}' | python3 -m json.tool | grep section_filter
curl -s "localhost:8000/api/words?band=all&sort_by=frequency" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['total'], d['words'][0]['frequency'] if d['words'] else 'empty')"

# 阅读理解 → 完形填空
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"cloze"}' | python3 -m json.tool | grep section_filter
curl -s "localhost:8000/api/words?band=all&sort_by=frequency" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['total'], d['words'][0]['frequency'] if d['words'] else 'empty')"

# 恢复全部
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"all"}' > /dev/null
```

- [ ] **Step 2: 验证缓存命中（切换回已访问题型零延迟）**

```bash
# 第一次切到翻译题 → 构建索引（稍慢）
time curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"translation"}' > /dev/null

# 第二次切到翻译题 → 缓存命中（极快）
time curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"translation"}' > /dev/null

# 恢复
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"all"}' > /dev/null
```

- [ ] **Step 3: 打开浏览器检查前端 UI**

浏览器打开 `http://localhost:5173/`，检查：
1. band 切换栏同一行有 "题型：" select
2. 切换到 "阅读理解 A+B" → 词表刷新，数量变化
3. 点击单词 → 弹窗例句标题包含 `阅读理解A` / `阅读理解B`
4. 弹窗中点击词性次数 → Popover 例句都是阅读理解
5. 切回 "全部题型" → 词表恢复

---

### Task 5 (Optional): 启动时后台预热四份索引

**Files:**
- Modify: `src/backend/main.py`

- [ ] **Step 1: 在 startup() 末尾加后台预热线程**

找到 `startup()` 函数末尾的 `print(f"✅ 从真题整理/ 加载了...")` 之后：

```python
    # 后台预热全部题型的索引缓存
    import threading
    def warmup():
        for sf in ["all", "cloze", "reading", "translation"]:
            _session_settings["section_filter"] = sf
            _build_index()
        _session_settings["section_filter"] = "all"
        _session_word_index = _build_index()
    threading.Thread(target=warmup, daemon=True).start()
```

- [ ] **Step 2: 重启后端并验证**

```bash
# 重启后端
kill $(lsof -ti:8000)
uvicorn src.backend.main:app --host 0.0.0.0 --port 8000 &
sleep 5

# 验证所有题型索引已缓存（切换应极快）
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"reading"}' > /dev/null
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"cloze"}' > /dev/null
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"translation"}' > /dev/null
curl -s -X PUT localhost:8000/api/settings -H "Content-Type: application/json" -d '{"section_filter":"all"}' > /dev/null
echo "所有题型切换完成"
```

---

### Task 6: commit

- [ ] **Step 1: 提交所有改动**

```bash
git add src/backend/main.py src/frontend/src/types.ts src/frontend/src/App.tsx src/frontend/src/index.css docs/02-design/题型筛选功能设计方案-2026-05-13.md docs/02-design/题型筛选功能实现计划-2026-05-13.md
git commit -m "feat: add section filter for cloze/reading/translation frequency stats"
```
