# 已解决问题清单

这是项目在重构与自查过程中已修复的问题汇总。

### 1. 核心数据丢失修复 (2026-05-11)
- **问题**：清洗逻辑过于严苛，导致 179 个真题单元中有 134 个内容变为空，高频词统计失效。
- **根因**：早年真题 TXT 缺少双换行符，且选项块紧跟在文章段落后，触发了激进的过滤规则。
- **修复**：
    - 优化 `clean_exam_sentences` 正则，支持全角括号 `［A］` 和 `(10 points)` 多种变体。
    - 增加「长段落保护」：若段落超过 500 字符且包含选项，尝试截断选项而非丢弃整个段落。
    - 改进数据加载逻辑：若无 `\n\n`，自动回退到按 `\n` 切分。
- **效果**：有效真题单元从 45 提升至 **143** 个。

### 2. 前端跨域与 API 连接修复 (2026-05-11)
- **问题**：前端无法获取数据，报跨域错误。
- **根因**：`App.tsx` 中硬编码了 `http://localhost:8000`，跳过了 Vite 配置的反向代理。
- **修复**：将 `API_BASE` 修改为空字符串 `""`，使其正确使用 Vite 代理。
- **效果**：前端与后端 API 成功连通。

### 3. 项目目录标准化重构 (2026-05-11)
- **问题**：根目录文件杂乱，代码、数据、文档混在一起。
- **修复**：
    - 建立了 `src/`, `data/`, `docs/`, `.ai/` 标准目录结构。
    - 迁移了 59 个文件，并更新了所有 Python 导入路径和 `start.sh` 启动脚本。
- **效果**：项目结构清晰，具备极强的可迁移性和 AI 协作友好性。

### 4. 词频阈值优化 (2026-05-11)
- **问题**：清洗掉 Directions 等冗余文本后，原本 0.5 的高频阈值导致高频词库为空。
- **修复**：将默认高频阈值下调至 **0.3**，中频阈值下调至 **0.1**。
- **效果**：现在进入系统能正常看到分级后的考研词汇。

### 5. 环境依赖与启动优化 (2026-05-11)
- **问题**：后端启动报 NLTK 资源缺失警告，且自动下载因权限问题报错。
- **修复**：手动补全 NLTK 资源，并注释掉会导致启动中断的自动下载代码，改由 `start.sh` 或手动维护。
- **效果**：后端启动平滑，无 Traceback。

---
*注：此文件仅记录重大修复，日常琐碎修改见 `docs/03-devlogs/`。*

### 6. 英语二真题集成与年份目录冲突修复 (2026-05-11)
- **问题**：用户上传了 2010-2023 英语二试卷，但系统不支持同一年的英语一/二共存，且 `.doc` 格式文件无法解析。
- **根因**：
    - 目录结构按 `{year}` 存放，同一年份只能有一套试卷。
    - `python-docx` 不支持旧式 `.doc` 格式。
    - `.docm`（宏启用文档）和个别损坏的 `.docx` 直接报错。
- **修复**：
    - 重构 `build_papers.py`，统一处理 `.pdf` / `.docx` / `.doc` 三种格式。
    - 新增 `parse_doc()` 通过 macOS `textutil` 命令行提取文字，`parse_docx()` 失败时自动回退。
    - 目录改为 `{year}_{exam_type}`（如 `2010_英语一`、`2010_英语二`），`_load_papers_from_disk` 同时加载。
    - `analyzer.py` 词频 dedup 由 `(year, section, pos)` 改为 `(year, section, exam_type, pos)` 五元组。
    - 2010 年以前不区分英语一/二，统一归一化为「英语一」。
- **效果**：成功处理 42/44 试卷（2 个 2025 扫描版 PDF 暂无法提取文字），section 数从 179 增至 192，文本总量 +32%。

### 7. 真题清洗逻辑大幅增强（题目、选项、试卷说明分离）(2026-05-11)
- **问题**：DOCX 真题中文章段落、试题题目、选项、中文考试说明全部粘连在一起，导致词频分析混入大量非文章字符。
- **修复**：
    - 新增 `CHINESE_METADATA_RE`：过滤"绝密★启用前"、"考生注意事项"、"科目代码"等中文说明。
    - 扩展 `QUESTION_START_RE` / `QUESTION_HINT_RE`：覆盖更多题型句式。
    - 新增 `QUESTION_ONLY_LINE_RE`：过滤编号试题行（如 `21. Which of the following...`）。
    - 新增 `OPTION_ONLY_LINE_RE`：过滤 `A. xxx`、`[A] xxx` 等单选选项行。
    - 新增 `BULLET_LINE_RE`：过滤子弹符号选项行（`• promote cooperation...`）。
    - 新增 `NUMBERED_QUESTION_RE`：过滤 `14. What the author tries to suggest...` 编号问题行。
    - `INSTRUCTION_RE` 加入更多中文考研说明关键词。
    - `UNICODE_CTRL_RE` 剥离 RTL 标记等不可见 Unicode 控制字符。
- **效果**：所有 192 个 section 均含有效内容，0 空 section，真题文章与题目选项实现有效分离。

### 8. 生词本功能（上传 xlsx/txt + 释义匹配 + 真题例句筛选）(2026-05-11)
- **需求**：用户有自己的生词本（xlsx 格式），希望上传后直接查看每个生词的释义和在历年真题中的出现情况。
- **实现**：
    - **后端**：3 个新 API — `POST /api/personal_vocab/upload`（支持 `.txt` / `.xlsx`，自动从 xlsx 所有单元格提取纯英文词并词形还原）、`GET /api/personal_words`（分页返回生词 + 墨墨释义 + 最短真题例句 + 墨墨例句）、`DELETE /api/personal_vocab`（清空）。
    - **前端**：新增 `PersonalVocabTable` 组件，四列表格（单词/词频/释义/真题例句）；顶部栏新增"上传生词本"按钮和"清除生词本"按钮；新增"生词本" Tab。
    - 生词本数据存储在内存中，存续期间一直可用。
- **效果**：用户上传 181 词的 xlsx 生词本，系统自动去重至 169 词，其中 131 词（77.5%）匹配到真题例句。

### 9. 生词本 Tab 切换 + 释义次数可点击 + 英语一/二区分显示 (2026-05-11)
- **问题 1**：进入生词本 Tab 后，高频/中频/低频切换 Tab 消失，无法切回。
- **修复 1**：Tab 栏从 `band === "personal"` 条件块中提至 toolbar 最外层，始终显示。
- **问题 2**：生词本释义后的 `(7次)` 不可点击，无法查看历年出现句子。
- **修复 2**：`PersonalVocabTable` 内置 `SenseDetailPopover` 弹窗组件，点击次数数字弹出历年真题句子列表。
- **问题 3**：所有例句和弹窗中不区分英语一/二。
- **修复 3**：`analyzer.py` 句子存储加入 `exam_type` 字段；`_load_papers_from_disk` 加载时 pre-2010 归一化为"英语一"；前端 4 个组件 5 处显示统一为 `英语一 · 2024 · 阅读理解A` 格式。
- **问题 4**：WordTable「常考例句」列只显示文字无出处。
- **修复 4**：改为两行显示 — 例句文本（截断 2 行）+ 底部蓝色出处行。

---
*注：此文件仅记录重大修复，日常琐碎修改见 `docs/03-devlogs/`。*
